# Python virtual environment — too large to upload, recreated with pip install
backend/venv/

# Python compiled files — auto-generated, not needed
backend/__pycache__/
backend/*.pyc

# React dependencies — too large to upload, recreated with npm install
frontend/node_modules/

# React production build — generated on demand, not source code
frontend/build/

# Environment variables — may contain secrets, never upload these
*.env
.env

# Windows junk files
Thumbs.db

# Mac junk files
.DS_Store

# Security — never upload tokens or passwords
GitHub Token for KindPDF.txt

#Backend Uploads folder, idk why but I was told to put this here
backend/uploads/

# Internal development notes — not relevant to public repo
*Starter_Prompt*
*starter_prompt*